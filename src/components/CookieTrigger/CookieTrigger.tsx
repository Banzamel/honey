import type {ReactNode} from 'react'

export interface CookieTriggerProps {
    label?: ReactNode
    className?: string
}

// TODO Phase 2: port MCookieTrigger.tsx — button to reopen the preferences
// drawer. Tiny component, mostly a styled <button> calling openPreferences().
export function CookieTrigger(_props: CookieTriggerProps) {
    return null
}
